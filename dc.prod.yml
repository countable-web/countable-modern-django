version: '3.3'

services:

  nginx:
    ports:
      - 80000:80
    restart: always
  web:
    restart: always
  db:
    restart: always
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  frontend:
    restart: always

